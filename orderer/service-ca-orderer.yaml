apiVersion: v1
kind: Service
metadata:
  name: svc-ca-orderer-org
  namespace: fabric
spec:
  type: ClusterIP
  selector:
    app: ca-orderer-org # Deployment'daki label ile eşleşir
  ports:
    - port: 7054
      targetPort: 7054
      protocol: TCP
